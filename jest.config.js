// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */

const config = {
  verbose: true,
  silent: true,
  testPathIgnorePatterns: ["./src/app/pages"],
};

module.exports = config;
